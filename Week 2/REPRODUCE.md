# Reproducibility Guide
This document provides instructions on how to replicate the analysis pipeline, including data cleaning, model estimation, and figure generation.

# 1. Prerequisites
This project uses **Python 3.12.3. To ensure the results are identical, you should use the exact package versions used during development.

# Environment Setup
1. Clone repository to your local.
2. Ensure you have the `requirements.txt` file.
3. Install dependencies:
   ```bash
   pip install -r requirements.txt